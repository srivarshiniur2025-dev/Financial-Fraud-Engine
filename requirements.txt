# Direct dependencies for Financial Fraud Intelligence Engine
# Compatible with Python 3.12 and 3.13 (Vercel-supported runtimes)
# Transitive Streamlit deps (altair, pillow, protobuf, tornado, watchdog)
# are installed automatically — do not pin them here (avoids resolver conflicts).

streamlit>=1.32.0,<2.0.0
pandas>=2.2.0,<4.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
matplotlib>=3.8.0,<4.0.0
joblib>=1.3.0,<2.0.0
