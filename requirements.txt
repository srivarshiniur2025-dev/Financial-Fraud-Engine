# Direct app dependencies only (Python 3.12 / 3.13, Linux wheels available).
# Do not pin Streamlit transitive deps (pillow, protobuf, altair, pyarrow, etc.).

streamlit>=1.32.0,<2.0.0
pandas>=2.2.0,<4.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
matplotlib>=3.8.0,<4.0.0
joblib>=1.3.0,<2.0.0
